div(class="text-center")
  table(class="table")
    thead
      tr
        th(colspan="2") Water
        th(colspan="3") Second sample
        th(class="fill") &nbsp;
      tr
        th Mass (g)
        th T<sub>i</sub>
        th Mass (g)
        th T<sub>i</sub>
        th SpHt (J/g-deg)
        th T<sub>f</sub>
    tbody
      tr
        td(name="fields")
        td(name="fields")
        td(name="fields")
        td(name="fields")
        td(name="fields")
        td(name="fields")
